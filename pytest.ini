[pytest]
markers =
    gpu: requires a self-hosted NVIDIA CUDA runner
    network: requires explicitly enabled external network access
    real_model: requires optional AI dependencies and a real model artifact
